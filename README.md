# Magic Patterns - Vite Template

This code was generated by [Magic Patterns](https://magicpatterns.com) for this design: [Source Design](https://www.magicpatterns.com/c/3vr4h9tzedoekgzqk6gyhr)

## Getting Started

1. Run `npm install`
2. Run `npm run dev`

## Supabase Setup

1. Install the SDK:
   `npm install @supabase/supabase-js`
2. Copy `.env.example` to `.env`.
3. Set your project values:
   - `VITE_SUPABASE_URL`
   - `VITE_SUPABASE_ANON_KEY`
4. Use the client from `src/lib/supabase.ts`.

Example:

```ts
import { supabase } from "./lib/supabase";

if (supabase) {
  const { data, error } = await supabase.from("your_table").select("*");
  console.log({ data, error });
}
```
